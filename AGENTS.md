# AGENTS

## Project Rules

- Future code changes must keep modifications minimal and targeted.
- Do not perform large refactors unless explicitly requested.
- Do not move or delete the original files under `samples/`.
- All paths must be written relative to the project root.
- Reuse the existing Ground Truth directory at `samples/groundTruth/`.
- All generated outputs must be saved under `results/`.
- Prefer small, readable Python modules over broad architectural changes.
